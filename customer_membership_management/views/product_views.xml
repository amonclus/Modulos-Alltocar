<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_membership_view" model="ir.ui.view">
        <field name="name">product.template.membership.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view" />
        <field name="arch" type="xml">
            <field name="uom_po_id" position="after">
                <field name="is_membership_product" groups="customer_membership_management.membership_manager"/>
            </field>

        </field>
    </record>

</odoo>